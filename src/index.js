import React from 'react';
import { render } from 'react-dom';
// import HelloWorld from './HelloWorld';
import _ from 'lodash';

const a = [true, null]
const b = a.filte(i => i === true)

render(
    // <HelloWorld />,
    document.getElementById('root')
);